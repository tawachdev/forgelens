{
  "name": "forgelens",
  "version": "0.2.0",
  "description": "Provider-agnostic local-first CLI that scans repositories and generates clean AI coding context files with evidence and confidence.",
  "type": "module",
  "bin": {
    "forgelens": "dist/cli.js"
  },
  "main": "dist/cli.js",
  "files": [
    "dist",
    "assets/forgelens-hero.png",
    "README.md",
    "CHANGELOG.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "tsup src/cli.ts --format esm,cjs --dts --clean --out-dir dist",
    "dev": "tsx src/cli.ts",
    "lint": "biome lint src tests scripts",
    "format:check": "biome check --changed --since=main --no-errors-on-unmatched --formatter-enabled=true --linter-enabled=false --organize-imports-enabled=false .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "release-check": "bash scripts/release-check.sh",
    "prepublishOnly": "pnpm release-check"
  },
  "keywords": [
    "ai",
    "codex",
    "cli",
    "repo",
    "static-analysis",
    "nextjs",
    "typescript",
    "agent-context"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/tawachdev/forgelens.git"
  },
  "homepage": "https://github.com/tawachdev/forgelens#readme",
  "bugs": {
    "url": "https://github.com/tawachdev/forgelens/issues"
  },
  "license": "MIT",
  "engines": {
    "node": ">=18.18.0"
  },
  "dependencies": {
    "commander": "^12.1.0",
    "fast-glob": "^3.3.3"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "@types/node": "^22.15.18",
    "tsup": "^8.5.0",
    "tsx": "^4.20.3",
    "typescript": "^5.8.3",
    "vitest": "^2.1.9"
  }
}
